# Usage

## Requirements
Go, SQLite3 

## Execution
Backend setup.
```bash
cd backend
go run main.go
```
For frontend just open .html file.

To read database queries.
```bash
cd backend
sqlite3 tasks.db
SELECT * FROM tasks;
```
